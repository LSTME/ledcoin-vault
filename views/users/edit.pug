extends ../layout

block content
  .container
    - result = result ? result : {value: user}

    form(method='POST' action=`/users/${user.$loki}`)
      p.title Edit user
      include _fields
      button.button.is-primary(type='submit') SAVE
