mixin message(message, header, style)
  .message(class=`is-${style}`)&attributes(attributes)
    .message-header= header
    .message-body
      if message
        = message
      if block
        block
